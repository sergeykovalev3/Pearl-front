# Copy to ".env.local" for local Next.js overrides (see ../ENVIRONMENT.md).
# Production: full URL with scheme, e.g. https://your-api.up.railway.app (no trailing slash).
# Hostname-only values are normalized (https:// prepended for non-localhost).

NEXT_PUBLIC_API_URL=http://localhost:4000
